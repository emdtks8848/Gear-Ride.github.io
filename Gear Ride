<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Gear Ride</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Arial', sans-serif;
      background-color: #2c2c2c; /* 아스팔트 배경 */
      color: white;
    }

    .container {
      display: flex;
      height: 100vh;
      width: 100%;
    }

    header {
      position: absolute;
      top: 20px;
      left: 30px;
      font-size: 28px;
      font-weight: bold;
      color: #fff;
      z-index: 10;
    }

    .background {
      flex: 2;
      display: flex;
      flex-direction: column;
      overflow: hidden;
    }

    .background img {
      width: 100%;
      height: 33.3%;
      object-fit: cover;
      opacity: 0.8;
    }

    .buttons {
      flex: 1;
      background-color: #1a1a1a;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      gap: 20px;
    }

    .buttons a {
      text-decoration: none;
      background-color: #0044cc; /* 블루 포인트 */
      color: white;
      padding: 15px 25px;
      border-radius: 8px;
      font-size: 18px;
      transition: background-color 0.3s;
      width: 200px;
      text-align: center;
    }

    .buttons a:hover {
      background-color: #cc0000; /* 레드 포인트 */
    }
  </style>
</head>
<body>
  <header>Gear Ride</header>
  <div class="container">
    <div class="background">
      <img src="city.jpg" alt="도시 이미지" />
      <img src="nature.jpg" alt="자연 이미지" />
      <img src="track.jpg" alt="트랙 이미지" />
    </div>
    <div class="buttons">
      <a href="#">오토바이 정보</a>
      <a href="#">장비 & 제품 정보</a>
      <a href="#">라이딩 코스</a>
      <a href="#">기타</a>
    </div>
  </div>
</body>
</html>
